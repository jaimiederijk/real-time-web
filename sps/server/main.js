import { Meteor } from 'meteor/meteor';

import '../imports/api/game.js';
//import '../imports/api/weather.js';

Meteor.startup(() => {
  
  //var weatherInterval = Meteor.setInterval(Meteor.call("checkWeather"), 1200000);//every 10 min get update
});
